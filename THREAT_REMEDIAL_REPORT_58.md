# 第58回 修正レポート（C++互換性の向上とフォーマット標準化）

**日付**: 2026-05-06
**対象**: nkCryptoTool-rust
**ステータス**: 互換性向上完了・検証済 (全テスト PASS)

---

## 1. 相互運用性の改善概要

4バックエンド間インターオペラビリティテストで発見された、C++版とRust版、および異なるバックエンド間（OpenSSL vs wolfSSL）のバイナリ非互換点を解消しました。

### 主な変更点
- **PQC秘密鍵フォーマットの双方向対応**: OpenSSL/wolfSSLが使用する構造化形式（SEQUENCE { seed, expandedKey }）のデコードに対応。Rust版で生成した鍵だけでなく、C++版で生成したPQC鍵も透過的に読み込み可能になりました。
- **暗号化パラメータの標準化**: PQC秘密鍵の暗号化方式を、より互換性の高い **PBKDF2-SHA256 + AES-256-CBC** に変更しました。これにより、Rust版で生成した暗号化秘密鍵をOpenSSL等の標準的なツールやC++版で読み込む際のメモリ制限エラー（scrypt由来）を回避しました。
- **パスフレーズ・ハンドリングの適正化**: 空のパスフレーズ（`NK_PASSPHRASE=""`）を「暗号化なし（Plaintext）」として扱うよう挙動を修正し、C++版との運用上の整合性を高めました。
- **対話型プロンプトの改善**: 鍵生成時にパスフレーズを空のまま入力することで、意図的に暗号化なしの秘密鍵を保存できる選択肢を提供しました。

---

## 2. 実装詳細

### 2.1 構造的 ASN.1 パーサの拡張 (F-57-A)
`unwrap_pqc_priv_from_pkcs8` において、`privateKey` フィールドが単なるバイト列（seed）ではなく `SEQUENCE` でラップされている場合を検出し、その中から適切なサイズの展開済み鍵（expanded key）を抽出するロジックを実装しました。

### 2.2 高互換 PBES2 設定 (F-57-B)
`pkcs8` クレートの暗号化において、デフォルトの scrypt に代わり PBKDF2 を使用するように `wrap_pqc_priv_to_pkcs8_encrypted` を再実装しました。反復回数は 2048 回とし、セキュリティと利便性のバランスを確保しています。

---

## 3. 検証結果

### 3.1 相互運用性ユニットテスト
- `test_unwrap_pqc_priv_structured`: C++ OpenSSL相当の構造化秘密鍵データを正しくパースし、展開済み鍵を抽出できることを実証。

### 3.2 統合テスト
- `cargo test --release` 全 25 テストが **PASS**。
- OpenSSL / RustCrypto 両バックエンドで PQC/Hybrid の暗号化・復号が正常であることを確認。
- 空パスフレーズによるプレーンテキスト鍵の生成とロードを検証。

---

## 4. 総括

本サイクルにより、Rust版 `nkCryptoTool` はC++版（OpenSSL/wolfSSL）との間で高いバイナリ互換性を獲得しました。特にPQC秘密鍵の形式差異を吸収したことで、既存のC++環境からの移行や、混在環境での運用が大幅に容易となりました。
