# 第59回 修正レポート（C++版のインターオペラビリティ向上と機能強化）

**日付**: 2026-05-06
**対象**: nkCryptoTool (C++)
**ステータス**: 互換性修正完了・検証済 (4-way テスト 100% PASS)

---

## 1. 相互運用性の改善概要

4バックエンド間（cpp_ossl / cpp_wolf / rust_ossl / rust_rc）の大規模なバイナリ互換テストに基づき、C++版の非互換ポイントを解消しました。これにより、Rust版を含む全ての組み合わせで 100% の相互運用性が確立されました。

### 主な修正内容
- **wolfSSL ECC バックエンドの修正 (P0)**: Shared Secret（ECDH 共有秘密）の導出において、OpenSSL と同一の 32バイト・ビッグエンディアン形式（X座標）を返すように修正。これまで wolfSSL 版でのみ復号できなかった問題を解決。
- **デフォルトアルゴリズムの統一 (P3)**: PQC の既定値を Rust版に合わせ、ML-KEM-768 および ML-DSA-65 へ変更。指定なしでのクロス利用時の OID 不一致エラーを防止。
- **アトミック書き込みの導入**: Rust版と同等の「一時ファイルへの書き出し + rename(2)」によるアトミックなファイル置換を C++ 版にも実装。
- **セキュリティ・ポリシの同期**: `--force` フラグによる上書き防止機能を実装し、不用意な鍵の消失を防ぐ設計を C++ 版にも適用。

---

## 2. 実装詳細

### 2.1 wolfSSL ECC 互換性の確保
`WolfSslBackend::eccDh` において、`wolfSSL_ECDH_compute_key` が返す X 座標がリーディングゼロによって 32 バイト未満になるケースに対応し、適切にパディングを行うロジックを追加しました。これにより、HKDF への入力が OpenSSL 版とビットレベルで一致するようになりました。

### 2.2 PQC ライブラリのアップデート
wolfSSL の PQC API 変更（Kyber -> ML-KEM）に対応するため、内部依存関係を `v5.9.1-stable` へ引き上げました。これに伴い、ML-KEM/ML-DSA の新 API を使用するようにバックエンド実装を刷新し、安定性と性能を向上させました。

### 2.3 秘密鍵抽出の安全性向上
Rust 側での修正（Report 58）と合わせ、C++ 版においても expanded key だけでなく seed-only 形式の秘密鍵からも透過的に鍵を復旧できるように `unwrapPqcDer` および各カプセル化・署名メソッドを強化しました。

---

## 3. 検証結果

### 3.1 4-way インターオペラビリティテスト
全 48 ケース（4 バックエンド × 3 モード × 4 バックエンド）において、全ての組み合わせで暗号化・復号が成功することを確認しました。
- **Mode ECC**: **16/16 PASS** (改善前: 9/16)
- **Mode PQC**: **16/16 PASS**
- **Mode Hybrid**: **16/16 PASS**

### 3.2 パフォーマンス (10.0 GiB 実測値)
| バックエンド | モード | 暗号化速度 | 復号速度 |
| :--- | :--- | :--- | :--- |
| **OpenSSL (C++)** | PQC (ML-KEM-768) | **~3.4 GiB/s** | **~3.4 GiB/s** |
| **wolfSSL (C++)** | PQC (ML-KEM-768) | **~2.2 GiB/s** | **~2.3 GiB/s** |

---

## 4. 総括

本サイクルをもって、`nkCryptoTool` プロジェクトにおける C++/Rust 間、および OpenSSL/wolfSSL/RustCrypto 間の「完全なバイナリ相互運用性」という目標が達成されました。
これにより、ユーザーはパフォーマンス要求や環境制約に応じて、安全性や互換性を損なうことなく最適な言語・バックエンドを自由に選択・混在させることが可能となりました。
